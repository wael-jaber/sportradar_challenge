import React from 'react';
import { Match } from 'scoreboard';

type AddMatchInputProps = {
  onAddMatch: (match: Match) => void;
};

export function AddMatchInput(props: AddMatchInputProps): React.ReactElement {
  return <div></div>;
}
